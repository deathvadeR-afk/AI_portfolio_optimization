# ML/RL
torch>=1.13.0,<2.2.0  # Compatible with Python 3.10
stable-baselines3>=1.8.0,<2.1.0  # Stable version for Python 3.10
gymnasium>=0.26.0,<0.30.0  # Compatible with Python 3.10

# Data
yfinance>=0.2.18,<0.3.0
pandas>=1.5.0,<2.1.0  # Stable version for Python 3.10
pandas-datareader>=0.10.0
fredapi>=0.5.0  # For FRED API access
numpy>=1.21.0,<1.26.0  # Compatible with Python 3.10
scipy>=1.9.0,<1.12.0

# Data Storage
tables>=3.7.0,<3.9.0  # For HDF5 support
pyarrow>=10.0.0,<15.0.0  # For Parquet support

# Visualization
plotly>=5.15.0,<5.19.0
streamlit>=1.25.0,<1.29.0

# Deployment & API
fastapi>=0.95.0,<0.105.0
uvicorn[standard]>=0.20.0,<0.25.0
pydantic>=1.10.0,<2.5.0
python-multipart>=0.0.5
gunicorn>=20.1.0,<21.0.0

# Additional Production Dependencies
scikit-learn>=1.0.0,<1.4.0
matplotlib>=3.5.0,<3.8.0
seaborn>=0.11.0,<0.13.0
requests>=2.28.0,<2.32.0
click>=8.1.0,<8.2.0
tqdm>=4.64.0,<4.67.0
pyyaml>=6.0,<6.1.0
joblib>=1.2.0,<1.4.0
# Docker Compose options:
# 1. Use system Docker Compose v2 (recommended): `docker compose` command
# 2. Use Python docker-compose package (legacy, has dependency conflicts):
docker-compose>=1.29.2  # Legacy Python package - has dependency conflicts but works
docker>=7.0.0  # Required by docker-compose
distro>=1.5.0  # Required by docker-compose
dockerpty>=0.4.1  # Required by docker-compose
docopt>=0.6.1  # Required by docker-compose
python-dotenv>=0.13.0  # Required by docker-compose
texttable>=0.9.0  # Required by docker-compose

# Testing
pytest>=7.1.0,<8.0.0

# Development
jupyter>=1.0.0
black>=22.0.0,<24.0.0  # Code formatting
mypy>=1.0.0,<1.7.0  # Type checking
